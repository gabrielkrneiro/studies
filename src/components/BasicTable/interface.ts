// "id": 1,
// "first_name": "Lolly",
// "last_name": "Holdron"

export interface User {
  id: number;
  firstName: string;
  lastName: string;
}

export interface UserTable {
  id: number;
  first_name: string;
  last_name: string;
}
